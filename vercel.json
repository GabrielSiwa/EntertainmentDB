{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "yarn install && prisma generate"
        }
      }
    ],
    "env": {
      "PRISMA_GENERATE_DATAPROXY": "true"
    }
  }